/**
 * @typedef {'note' | 'file'} ResourceType
 * Enum for system resource types
 * @readonly
 */
const resources = Object.freeze({
    NOTE: "note",
    FILE: "file"
});

module.exports = resources;
