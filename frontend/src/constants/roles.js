const roles = Object.freeze({
    OWNER: "owner",
    VIEWER: 'viewer',
    EDITOR: 'editor'
});

const permissionActions = Object.freeze({
    REMOVE_ACCESS: 'remove_access'
});

export {roles, permissionActions};
