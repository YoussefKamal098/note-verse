export const SOCKET_EVENTS = Object.freeze({
    NEW_NOTIFICATION: 'new_notification',
    NOTE: Object.freeze({
        JOIN: 'note_join',
        LEAVE: 'note_leave',
        UPDATE: 'note_update',
    }),
});
